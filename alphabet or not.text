//program to check whether the give character is alphabet or not
#include<stdio.h>
void main()
{
	char ch;
	printf("enter the character:");
	scanf("%c",&ch);
	if(ch>='a'&&ch<='z'||ch>='A'&&ch<='Z')
	printf("%c is a alphabet",ch);
	else
	printf("%c is not a alphabet",ch);
}